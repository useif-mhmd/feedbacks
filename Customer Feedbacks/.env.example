# Database
MONGODB_URI=mongodb://localhost:27017/feedback-automation

# Server
PORT=8080
NODE_ENV=development

# WhatsApp (configured through UI)
# No additional config needed for whatsapp-web.js

# SMTP (configured through UI)
# SMTP settings are managed through the dashboard

# Optional: File upload limits
MAX_FILE_SIZE=10485760

# Optional: Rate limiting
RATE_LIMIT_WINDOW=900000
RATE_LIMIT_MAX=100
